# Oral Narrative Preservation System - Python Dependencies
# Install: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Computer Vision ────────────────────────────────────────
opencv-python>=4.9.0
deepface>=0.0.92          # Facial attribute analysis (emotion, age, gender, identity)
# tf-keras>=2.15.0          # Required by DeepFace backend

# ── Audio Processing ───────────────────────────────────────
librosa>=0.10.1           # Pitch, MFCC, spectral features, chroma
soundfile>=0.12.1         # Audio I/O
webrtcvad>=2.0.10         # Voice Activity Detection
pyaudio>=0.2.14           # Real-time audio capture (optional)

# ── Machine Learning ───────────────────────────────────────
torch>=2.2.0              # PyTorch for multimodal fusion model
torchvision>=0.17.0       # Vision transforms
torchaudio>=2.2.0         # Audio transforms
pytorch-grad-cam>=1.5.0   # Gradient-weighted CAM visualization
scikit-learn>=1.4.0       # Feature engineering, preprocessing, scaling
numpy>=1.26.0
pandas>=2.2.0

# ── Database & Storage ─────────────────────────────────────
sqlalchemy>=2.0.0         # ORM (production: replace in-memory dict)
alembic>=1.13.0           # DB migrations
pymongo>=4.6.0            # MongoDB (optional: for document storage)
redis>=5.0.0              # Caching layer (optional)

# ── Speech Recognition ─────────────────────────────────────
openai-whisper>=20231117  # ASR transcription
# faster-whisper>=0.10.0  # Drop-in accelerated Whisper alternative

# ── Utilities ──────────────────────────────────────────────
pydantic>=2.5.0
python-jose>=3.3.0        # JWT auth tokens
passlib>=1.7.4            # Password hashing
httpx>=0.27.0
aiofiles>=23.2.1
tqdm>=4.66.0
Pillow>=10.2.0

# ── Visualization ──────────────────────────────────────────
matplotlib>=3.8.0         # Grad-CAM plots
seaborn>=0.13.0

# ── Production extras ──────────────────────────────────────
# gunicorn>=21.2.0
# celery>=5.3.0            # Distributed task queue for heavy processing
# boto3>=1.34.0            # AWS S3 for media storage
