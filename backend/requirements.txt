# Oral Narrative Preservation System - Python Dependencies
# Install: pip install -r requirements.txt
# Optimized for standard mode (~1.5GB total with CPU-only PyTorch)

# ── Web Framework ──────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Computer Vision ────────────────────────────────────────
opencv-python-headless>=4.9.0
deepface>=0.0.92
tf-keras>=2.16.0

# ── Audio Processing ───────────────────────────────────────
librosa>=0.10.1
soundfile>=0.12.1
webrtcvad>=2.0.10

# ── Machine Learning (CPU-only PyTorch) ────────────────────
# CPU-only PyTorch (saves ~1.5GB vs CUDA version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
faiss-cpu>=1.7.4

# ── Database & Storage ─────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
redis>=5.0.0
aiosqlite>=0.19.0

# ── Speech Recognition ─────────────────────────────────────
faster-whisper>=1.0.0

# ── Utilities ──────────────────────────────────────────────
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-jose>=3.3.0
passlib>=1.7.4
bcrypt>=4.0.0
email-validator>=2.1.0
httpx>=0.27.0
aiofiles>=23.2.1
tqdm>=4.66.0
Pillow>=10.2.0

# ── Visualization ──────────────────────────────────────────
matplotlib>=3.8.0

# ── Production extras ──────────────────────────────────────
celery>=5.3.0
flower>=2.0.0
fastapi-limiter>=0.1.6
psutil>=5.9.0

# ── Testing ────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
